{
    "tasks": [
        {
            "type": "shell",
            "label": "cleanup",
            "command": "rm ${fileDirname}/${fileBasenameNoExtension} 2"
        },
        {
            "type": "cppbuild",
            "label": "compile C",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-lm"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
